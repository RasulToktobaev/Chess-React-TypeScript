import React from 'react';

interface LO

const LostFigures = ({title, figures}) => {
    return (
        <div className="lost">
            <h3>{title}</h3>
        </div>
    );
};

export default LostFigures;